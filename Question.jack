class Question {

    field String question;
    field Array options;
    field char answer;

    constructor Question new(String q, Array o, char a) {
        let question = q;
        let options = o;
        let answer = a;
        return this;
    }

    method void dispose() {
        do Memory.deAlloc(this);
        return;
    }
}
